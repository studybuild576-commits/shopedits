// Is page ka code abhi ke liye placeholder hai, ise baad mein banayenge.
import 'package:flutter/material.dart';

class IDCardMakerPage extends StatelessWidget {
  const IDCardMakerPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('ID Card Maker')),
      body: const Center(
        child: Text('ID Card Maker Feature Coming Soon!'),
      ),
    );
  }
}
